/** Single source of truth for all user-visible tool name strings. */
export const TOOL_NAME = "iNoU";
/** Lowercase binary/command name — constrained by npm and shell entry points. */
export const TOOL_CMD = "inuo";
/** Shell prompt prefix used to identify user-command lines in log streams. */
export const TOOL_PROMPT = `${TOOL_NAME} >`;
