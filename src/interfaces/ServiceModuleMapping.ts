export interface ServiceModuleMapping {
  /** Verb + Object key (e.g., 'Consult+Geotechnical survey') */
  pairKey: string;
  
  /** Discrete service module directory/file path */
  modulePath: string;
}
